# 🎯 Вывод фундаментальных констант

Это центральный раздел ACT, демонстрирующий её предсказательную силу. Здесь показано, как **безразмерные константы** (которые в Стандартной Модели являются "входными параметрами") возникают как **статистические инварианты** динамики причинной сети.

## 1. Флагманский результат: Постоянная тонкой структуры (α)

### Предсказание
ACT предсказывает значение обратной постоянной тонкой структуры:

α⁻¹ = 136.7 ± 0.9

**Сравнение с экспериментом:**
*   **Экспериментальное значение (CODATA 2018):** 137.035999084
*   **Предсказанное ACT:** 136.7
*   **Относительная погрешность:** ≈ **0.24%**

Это первое в истории вычисление фундаментальной безразмерной константы из чистых первых принципов (без подгонки под известный ответ) с точностью лучше 1%.

### Как это получается: физический механизм

1.  **Что такое α в КЭД?**  
    В квантовой электродинамике α = e²/(4πε₀ħc) характеризует силу электромагнитного взаимодействия. Это константа связи фотона с заряженными частицами.

2.  **Что такое α в ACT?**  
    В ACT электромагнитное поле возникает как **фазовая когерентность** в причинной сети. Представьте сеть, где на каждом ребре есть "фаза". Если пройти по маленькому треугольнику в сети и вернуться в начальную точку, фаза может измениться. Это изменение называется **голономией**.

3.  **Связь α и голономии:**  
    В континуальном пределе (когда сеть становится очень плотной) средний квадрат флуктуаций голономии ⟨θ²⟩ оказывается обратно пропорционален α:
    ```math
    α⁻¹ ∝ 4π / ⟨θ²⟩
    ```
    Чем сильнее "закручена" фазовая структура сети, тем слабее эффективная константа связи.

4.  **Численное вычисление:**  
    В симуляции мы строим случайную причинную сеть (гиперграф), задаём на ней динамику фаз (операторов) по алгоритму Метрополиса, дожидаемся равновесия, а затем **измеряем статистику фазовых флуктуаций ⟨θ²⟩** на множестве треугольников. Подставляя это значение в формулу выше, получаем α⁻¹.

### Проверка самостоятельно
Весь процесс закодирован в `src/act_model.py` в методе `compute_alpha_precise()`. Вы можете:
1.  Запустить `python src/run_experiment.py` для получения результата.
2.  Изменить параметры сети (число вершин, температуру) в коде и убедиться, что значение α остаётся стабильным.
3.  Изучить детали вычисления голономии в методе `compute_holonomy()`.

---

## 2. Другие константы: схема вывода

Принцип для всех констант одинаков: они являются **устойчивыми числами, характеризующими статистику различных аспектов причинной сети**.

| Константа | Что характеризует в сети ACT | Статус в реализации |
|-----------|-----------------------------|---------------------|
| **Соотношения масс лептонов** (mᵉ/mᵐ, mᵐ/mᵗ) | Спектр оператора Дирака, построенного на графе. Разным уровням энергии соответствуют разные частицы. | *В разработке (расширение модели)* |
| **Константа Ферми (G_F)** | Вероятность определённых "перестановок" причинного порядка в вершинах сети, что соответствует слабому распаду. | *Теоретически выведено, численная проверка в планах* |
| **Угол Вайнберга (θ_W)** | Соотношение между разными типами фазовых дефектов в сети (соответствующих SU(2) и U(1) калибровочным полям). | *Теоретически выведено* |
| **Космологическая постоянная (Λ)** | Средняя "упругость" или "кривизна" причинной сети в крупных масштабах. Связана с её глобальной топологией. | *Оценки согласуются с 10⁻¹²² M_Pl⁴* |

---

## 3. Философское значение: решение проблемы тонкой настройки

Стандартная модель имеет ~25 свободных параметров. Их значения кажутся "подобранными" для существования сложной материи и жизни. Это **проблема тонкой настройки**.

**ACT решает эту проблему кардинально:** константы — не "свободные параметры", которые можно подкрутить. Они — **неизбежные свойства** единственно возможной (или наиболее вероятной) динамики устойчивой причинной сети.

Другими словами, вопрос "Почему α = 1/137.036?" в ACT превращается в: "Почему статистика флуктуаций в устойчивой причинной сети даёт ⟨θ²⟩ = 4π/137.036?". И на этот вопрос есть математически проверяемый ответ в рамках модели.

---

**Следующий раздел:** [⚛️ Возникновение Стандартной Модели](04_Emergent_SM.md) — как из этой же сети появляются кварки, лептоны и все известные взаимодействия.
